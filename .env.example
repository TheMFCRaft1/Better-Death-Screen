# Kopiere diese Datei nach .env und trage deine Werte ein.
# .env wird von Git ignoriert — niemals committen.
#
#   cp .env.example .env

# ---------------------------------------------------------------------------
# Publishing aktivieren (Pflicht für upload-Tasks)
# ---------------------------------------------------------------------------
PUBLISH_ENABLED=false

# ---------------------------------------------------------------------------
# Modrinth — https://modrinth.com/settings/personal-access-tokens
# Projekt ZUERST auf https://modrinth.com/mod/create anlegen, dann Slug oder UUID hier:
#   (Slug steht in der URL: modrinth.com/mod/DEIN-SLUG)
# ---------------------------------------------------------------------------
MODRINTH_TOKEN=
MODRINTH_PROJECT_ID=better-death-screen

# ---------------------------------------------------------------------------
# CurseForge — https://console.curseforge.com/#/api-tokens
# WICHTIG: Nur den API-Token aus der Console kopieren (langer Klartext-Key),
#   NICHT dein Passwort und kein $2a$10$-Hash!
# CURSEFORGE_PROJECT_ID = numerische Projekt-ID (z. B. 1555631)
# ---------------------------------------------------------------------------
CURSEFORGE_TOKEN=
CURSEFORGE_PROJECT_ID=

# ---------------------------------------------------------------------------
# Release-Metadaten (optional)
# ---------------------------------------------------------------------------
# STABLE | BETA | ALPHA
RELEASE_TYPE=STABLE

# Changelog-Text für den Upload (Markdown erlaubt)
CHANGELOG=Release 1.0.0

# ---------------------------------------------------------------------------
# GitHub Actions (Secrets im Repository hinterlegen, nicht in .env committen)
# ---------------------------------------------------------------------------
# MODRINTH_TOKEN
# CURSEFORGE_TOKEN
# CURSEFORGE_PROJECT_ID
# PUBLISH_ENABLED=true
